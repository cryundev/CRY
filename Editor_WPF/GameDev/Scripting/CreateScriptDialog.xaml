<Window x:Class="Editor_WPF.GameDev.Scripting.CreateScriptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Style="{StaticResource ProjectBrowserDialogStyle}"
        Title="CreateScriptDialog" Width="400" Height="250"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">

    <Border Margin="10" HorizontalAlignment="Stretch"
            Background="{StaticResource Editor.Window.GrayBrush3}"
            CornerRadius="3">
        <Border.Effect>
            <DropShadowEffect Opacity="0.6" BlurRadius="15" ShadowDepth="2" />
        </Border.Effect>

        <Grid Margin="32,0">
            <Grid x:Name="BusyAnimation"
                  HorizontalAlignment="Left" VerticalAlignment="Center"
                  Visibility="Hidden">
                <Grid.Resources>
                    <SolidColorBrush x:Key="BusyBrush" Color="{StaticResource Editor.FontColor}" />
                </Grid.Resources>

                <Ellipse Width="164" Height="164" Margin="-155,0,0,0"
                         HorizontalAlignment="Center" VerticalAlignment="Center"
                         Stroke="{StaticResource BusyBrush}"
                         StrokeThickness="1"
                         RenderTransformOrigin="0.5,0.5" />

                <Ellipse Width="154" Height="154" Margin="-155,0,0,0"
                         HorizontalAlignment="Center" VerticalAlignment="Center"
                         Stroke="{StaticResource BusyBrush}"
                         StrokeThickness="10"
                         StrokeDashArray="0.3,0.22"
                         RenderTransformOrigin="0.5,0.5">
                    <Ellipse.RenderTransform>
                        <RotateTransform />
                    </Ellipse.RenderTransform>

                    <Ellipse.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="(Ellipse.RenderTransform).(RotateTransform.Angle)"
                                                     To                       ="-360"
                                                     Duration                 ="0:0:2"
                                                     RepeatBehavior           ="Forever" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Ellipse.Triggers>
                </Ellipse>

                <Ellipse Width="174" Height="174" Margin="-155,0,0,0"
                         HorizontalAlignment="Center" VerticalAlignment="Center"
                         Stroke="{StaticResource BusyBrush}"
                         StrokeThickness="5"
                         StrokeDashArray="0.5,1.186"
                         RenderTransformOrigin="0.5,0.5">
                    <Ellipse.RenderTransform>
                        <RotateTransform />
                    </Ellipse.RenderTransform>

                    <Ellipse.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="(Ellipse.RenderTransform).(RotateTransform.Angle)"
                                                     From                     ="360"
                                                     To                       ="0"
                                                     Duration                 ="0:0:10"
                                                     RepeatBehavior           ="Forever" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Ellipse.Triggers>
                </Ellipse>
                <Polyline Stroke="{StaticResource BusyBrush}">
                    <Polyline.Points>
                        <Point X="160" Y="35" />
                        <Point X="180" Y="20" />
                        <Point X="340" Y="20" />
                    </Polyline.Points>
                </Polyline>

                <TextBlock Text="Create new script..." Margin="0,20,0,0"
                           HorizontalAlignment="Right" VerticalAlignment="Top"
                           Foreground="White">
                    <TextBlock.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                     From                     ="1"
                                                     To                       ="0"
                                                     Duration                 ="0:0:1"
                                                     RepeatBehavior           ="Forever"
                                                     AutoReverse              ="True" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </TextBlock.Triggers>
                </TextBlock>
            </Grid>

            <Grid Margin="32,0">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <TextBlock x:Name="MessageTextBlock"
                           Margin="0,10,0,0"
                           HorizontalAlignment="Center" VerticalAlignment="Top"
                           TextWrapping="Wrap"
                           Foreground="{StaticResource Editor.RedBrush}" />

                <StackPanel VerticalAlignment="Center">
                    <DockPanel>
                        <TextBlock Text="Script Name" Width="72" Style="{StaticResource LightTextBlockStyle}" />
                        <TextBox x:Name="ScriptNameTextBox" TextChanged="OnScriptNameTextBoxTextChanged" />
                    </DockPanel>

                    <DockPanel Margin="0,5,0,0">
                        <TextBlock Text="Script Path" Width="72" Style="{StaticResource LightTextBlockStyle}" />
                        <TextBox x:Name     ="ScriptPathTextBox"
                                 Text       ="Source\"
                                 TextChanged="OnScriptPathTextBoxTextChanged" />
                    </DockPanel>
                </StackPanel>

                <StackPanel Margin="0,0,0,20"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right" VerticalAlignment="Bottom">
                    <Button Content="Create" Width="64" Margin="32,0" IsDefault="True" Click="OnCreateButtonClicked" />
                    <Button Content="Cancel" Width="64" IsCancel="True" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
