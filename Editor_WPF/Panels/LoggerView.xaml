<UserControl x:Class="Editor_WPF.Panels.LoggerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:utilities="clr-namespace:Editor_WPF.Utilities"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <Style TargetType="{x:Type ToggleButton}">
            <Setter Property="FocusVisualStyle"           Value="{x:Null}" />
            <Setter Property="BorderBrush"                Value="{StaticResource Editor.Window.GrayBrush4}" />
            <Setter Property="BorderThickness"            Value="1" />
            <Setter Property="Width"                      Value="18" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment"   Value="Center" />
            <Setter Property="Padding"                    Value="1" />
            <EventSetter Event="Click" Handler="OnMessageFilterButtonClicked" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border x:Name             ="outerBorder"
                                BorderBrush        ="{TemplateBinding BorderBrush}"
                                BorderThickness    ="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">
                            <Border x:Name="innerBorder" Background="{TemplateBinding Background}" Margin="1" />
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="outerBorder" Property="BorderBrush" Value="{Binding Background, ElementName=innerBorder}" />
                            </Trigger>

                            <Trigger Property="IsChecked" Value="False">
                                <Setter TargetName="innerBorder" Property="Opacity" Value="0.5" />
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="innerBorder" Property="Opacity" Value="0.2" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer x:Name                       ="scrollViewer"
                      Background                   ="{StaticResource Editor.Window.GrayBrush2}"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility  ="Auto">
            <ItemsControl ItemsSource="{Binding Source={x:Static utilities:Logger.FilteredMessages}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <DockPanel ToolTip="{Binding MetaData}">
                            <DockPanel.Style>
                                <Style TargetType="DockPanel">
                                    <Setter Property="Background" Value="Transparent" />
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background">
                                                <Setter.Value>
                                                    <SolidColorBrush Color="{StaticResource Editor.Selected.BackgroundColor}" Opacity="0.5" />
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DockPanel.Style>

                            <TextBlock Text="{Binding Time, StringFormat=hh:mm:ss}" Margin="5,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="LightGreen" />

                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding MessageType}" Value="{x:Static utilities:MessageType.Warning}">
                                                <Setter Property="Foreground" Value="#ffffbb22" />
                                            </DataTrigger>

                                            <DataTrigger Binding="{Binding MessageType}" Value="{x:Static utilities:MessageType.Error}">
                                                <Setter Property="Foreground" Value="#ffff4455" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <TextBlock Text        ="{Binding Message}"
                                       TextWrapping="Wrap"
                                       Style       ="{StaticResource LightTextBlockStyle}" />
                        </DockPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Border Height="32" VerticalAlignment="Top" Background="{StaticResource Editor.Window.GrayBrush3}">
            <Border.Effect>
                <DropShadowEffect Opacity="0.3" BlurRadius="10" ShadowDepth="0" />
            </Border.Effect>

            <StackPanel Margin="4,0"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="Clear" Margin="1,0" Focusable="False" Click="OnClearButtonClicked" />

                <ToggleButton x:Name="toggleInfo"
                              Content="info" Margin="1,0"
                              IsChecked="True"
                              ToolTip="Toggle information messages"
                              Background="{StaticResource Editor.GreenBrush}" />

                <ToggleButton x:Name="toggleWarning"
                              Content="warning" Margin="1,0"
                              IsChecked="True"
                              ToolTip="Toggle warning messages"
                              Background="{StaticResource Editor.OrangeBrush}" />

                <ToggleButton x:Name="toggleError"
                              Content="error" Margin="1,0"
                              IsChecked="True"
                              ToolTip="Toggle error messages"
                              Background="{StaticResource Editor.RedBrush}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
