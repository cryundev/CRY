<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:assetBrowser="clr-namespace:Editor_WPF.AssetBrowser"
                    xmlns:control="clr-namespace:Editor_WPF.Controls">

    <Style x:Key="AssetBrowserPanelBorderStyle" TargetType="{x:Type Border}">
        <Setter Property="Background" Value="{StaticResource Editor.Window.GrayBrush2}" />
        <Setter Property="BorderBrush" Value="{StaticResource Editor.Window.GrayBrush4}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <Style x:Key="AssetBrowserInsetBorderStyle" TargetType="{x:Type Border}" BasedOn="{StaticResource AssetBrowserPanelBorderStyle}">
        <Setter Property="Background" Value="{StaticResource Editor.Window.GrayBrush3}" />
        <Setter Property="BorderBrush" Value="{StaticResource Editor.Window.GrayBrush5}" />
        <Setter Property="CornerRadius" Value="3" />
    </Style>

    <Style x:Key="AssetBrowserTitleTextStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource Editor.FontBrush}" />
    </Style>

    <Style x:Key="AssetBrowserMutedTextStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Foreground" Value="{StaticResource Editor.Disable.FontBrush}" />
    </Style>

    <Style x:Key="AssetTileNameTextStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource Editor.FontBrush}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}}" Value="True">
                <Setter Property="Foreground" Value="White" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="AssetTileMutedTextStyle" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource AssetBrowserMutedTextStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}}" Value="True">
                <Setter Property="Foreground" Value="{StaticResource Editor.FontBrush}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <BitmapImage x:Key="AssetFolderIconImage"
                 CacheOption="OnLoad"
                 UriSource="/Resources/Icons/AssetBrowser/folder.png" />

    <BitmapImage x:Key="AssetObjectIconImage"
                 CacheOption="OnLoad"
                 UriSource="/Resources/Icons/AssetBrowser/asset.png" />

    <BitmapImage x:Key="AssetFileIconImage"
                 CacheOption="OnLoad"
                 UriSource="/Resources/Icons/AssetBrowser/file.png" />

    <Style x:Key="AssetBrowserContextMenuStyle" TargetType="{x:Type ContextMenu}">
        <Setter Property="Background" Value="{StaticResource Editor.Window.GrayBrush3}" />
        <Setter Property="BorderBrush" Value="{StaticResource Editor.Window.GrayBrush5}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="2" />
    </Style>

    <Style x:Key="AssetBrowserMenuItemStyle" TargetType="{x:Type MenuItem}">
        <Setter Property="Foreground" Value="{StaticResource Editor.FontBrush}" />
        <Setter Property="Background" Value="{StaticResource Editor.Window.GrayBrush3}" />
        <Setter Property="Padding" Value="14,5" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type MenuItem}">
                    <Border x:Name="ItemBorder"
                            Background="{TemplateBinding Background}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter ContentSource="Header"
                                          Margin="{TemplateBinding Padding}"
                                          RecognizesAccessKey="True" />
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource Editor.Selected.BackgroundBrush}" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource Editor.Disable.FontBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AssetTreeExpanderStyle" TargetType="{x:Type ToggleButton}">
        <Setter Property="Width" Value="16" />
        <Setter Property="Height" Value="20" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Path x:Name="Arrow"
                          Width="6" Height="8"
                          HorizontalAlignment="Center" VerticalAlignment="Center"
                          Stroke="{StaticResource Editor.Disable.FontBrush}"
                          StrokeThickness="1"
                          Data="M 1 0 L 5 4 L 1 8" />

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Arrow" Property="RenderTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="90" CenterX="3" CenterY="4" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Arrow" Property="Stroke" Value="{StaticResource Editor.FontBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AssetTreeViewItemStyle" TargetType="{x:Type TreeViewItem}">
        <Setter Property="Foreground" Value="{StaticResource Editor.FontBrush}" />
        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TreeViewItem}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition />
                        </Grid.RowDefinitions>

                        <Border x:Name="HeaderBorder"
                                Padding="3,1"
                                Background="Transparent"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="18" />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>

                                <ToggleButton x:Name="Expander"
                                              Grid.Column="0"
                                              Style="{StaticResource AssetTreeExpanderStyle}"
                                              IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" />

                                <ContentPresenter Grid.Column="1"
                                                  x:Name="PART_Header"
                                                  ContentSource="Header"
                                                  HorizontalAlignment="Stretch" />
                            </Grid>
                        </Border>

                        <ItemsPresenter x:Name="ItemsHost" Grid.Row="1" Margin="18,0,0,0" />
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="Expander" Property="Visibility" Value="Hidden" />
                        </Trigger>

                        <Trigger Property="IsExpanded" Value="False">
                            <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed" />
                        </Trigger>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="HeaderBorder" Property="Background" Value="{StaticResource Editor.Window.GrayBrush4}" />
                        </Trigger>

                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="HeaderBorder" Property="Background" Value="{StaticResource Editor.Selected.BackgroundBrush}" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <HierarchicalDataTemplate DataType="{x:Type assetBrowser:AssetFolderNodeViewModel}"
                              ItemsSource="{Binding Children}">
        <StackPanel Orientation="Horizontal" Margin="0,1">
            <Image Width="16" Height="14" Margin="0,1,3,0"
                   Source="{StaticResource AssetFolderIconImage}" />

            <TextBlock Text="{Binding Name}"
                       TextTrimming="CharacterEllipsis"
                       Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type TreeViewItem}}}" />
        </StackPanel>
    </HierarchicalDataTemplate>

    <Style x:Key="AssetTileListBoxItemStyle" TargetType="{x:Type ListBoxItem}">
        <Setter Property="Width" Value="112" />
        <Setter Property="Height" Value="98" />
        <Setter Property="Margin" Value="4" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="3"
                            SnapsToDevicePixels="True">
                        <ContentPresenter Margin="{TemplateBinding Padding}" />
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Editor.Window.GrayBrush4}" />
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Editor.Window.GrayBrush5}" />
                        </Trigger>

                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Editor.Selected.BackgroundBrush}" />
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Editor.BlueBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <DataTemplate DataType="{x:Type assetBrowser:AssetBrowserItem}">
        <Grid Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="44" />
                <RowDefinition />
                <RowDefinition Height="18" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Width="42" Height="36"
                    HorizontalAlignment="Center"
                    Style="{StaticResource AssetBrowserInsetBorderStyle}">
                <Grid>
                    <Image x:Name="FolderIcon" Width="32" Height="32"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Source="{StaticResource AssetFolderIconImage}"
                           Visibility="Collapsed" />

                    <Image x:Name="AssetIcon" Width="30" Height="30"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Source="{StaticResource AssetObjectIconImage}"
                           Visibility="Collapsed" />

                    <Image x:Name="FileIcon" Width="28" Height="30"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Source="{StaticResource AssetFileIconImage}"
                           Visibility="Collapsed" />
                </Grid>
            </Border>

            <TextBlock Grid.Row="1"
                       Text="{Binding Name}"
                       TextAlignment="Center"
                       TextTrimming="CharacterEllipsis"
                       TextWrapping="NoWrap"
                       Style="{StaticResource AssetTileNameTextStyle}"
                       ToolTip="{Binding ProjectRelativePath}" />

            <TextBlock Grid.Row="2"
                       Text="{Binding DetailText}"
                       HorizontalAlignment="Center"
                       TextTrimming="CharacterEllipsis"
                       Style="{StaticResource AssetTileMutedTextStyle}" />
        </Grid>

        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding Kind}" Value="{x:Static assetBrowser:AssetBrowserItemKind.Folder}">
                <Setter TargetName="FolderIcon" Property="Visibility" Value="Visible" />
            </DataTrigger>

            <DataTrigger Binding="{Binding Kind}" Value="{x:Static assetBrowser:AssetBrowserItemKind.Asset}">
                <Setter TargetName="AssetIcon" Property="Visibility" Value="Visible" />
            </DataTrigger>

            <DataTrigger Binding="{Binding Kind}" Value="{x:Static assetBrowser:AssetBrowserItemKind.File}">
                <Setter TargetName="FileIcon" Property="Visibility" Value="Visible" />
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>
</ResourceDictionary>
