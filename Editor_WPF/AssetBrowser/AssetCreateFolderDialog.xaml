<Window x:Class="Editor_WPF.AssetBrowser.AssetCreateFolderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Style="{StaticResource ProjectBrowserDialogStyle}"
        Title="Create Folder" Width="360" Height="180"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">

    <Border Margin="10"
            Background="{StaticResource Editor.Window.GrayBrush3}"
            CornerRadius="3">
        <Border.Effect>
            <DropShadowEffect Opacity="0.6" BlurRadius="15" ShadowDepth="2" />
        </Border.Effect>

        <Grid Margin="24,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="MessageTextBlock"
                       Grid.Row="0"
                       MinHeight="18"
                       TextWrapping="Wrap"
                       Foreground="{StaticResource Editor.RedBrush}" />

            <DockPanel Grid.Row="1" VerticalAlignment="Center">
                <TextBlock Text="Name" Width="54"
                           Style="{StaticResource LightTextBlockStyle}" />

                <TextBox x:Name="FolderNameTextBox"
                         Text="New Folder"
                         TextChanged="OnFolderNameTextBoxTextChanged" />
            </DockPanel>

            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="Create" Width="64" Margin="0,0,8,0"
                        IsDefault="True"
                        Click="OnCreateButtonClicked" />

                <Button Content="Cancel" Width="64"
                        IsCancel="True" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
